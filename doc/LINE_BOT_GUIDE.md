# 📱 Line Bot 使用指南 - 策略切換版

> **更新日期**: 2026-02-13  
> **版本**: V35 策略解耦 + 多策略切換

---

## 🎯 核心功能

### 1. 策略切換指令（✨ 新功能）

Line Bot 現在支援 **動態切換策略**，讓你選擇最適合的選股策略！

| 指令 | 切換至策略 | 特色 |
|-----|----------|------|
| `切換V30` 或 `切v30` | V31 混合策略 | 均線多頭 + RSI + AI 排名（ROI 22.86%） |
| `切換V33` 或 `低波動` | V33 低波動 | 波動率 NATR < 3.5%，穩健成長優先 |
| `切換V34` 或 `飆股` | V34 雙渦輪飆股 | 營收成長 + 60日高突破（支援自訂門檻） |
| `切換V35` 或 `創新` | V35 經營效益 | 營業利益率 + 營收成長（支援自訂門檻） |
| `查看策略` | - | 顯示當前策略 + 停損/停利/持有天數 |

#### 使用範例

```
使用者: 切換V33
Bot: 🔄 已切換至 V33 低波動策略

特色：
• 波動率 < 4%
• 穩定成長優先
• 適合保守型投資者

💡 輸入「推薦」開始選股
```

---

### 2. 選股功能

| 指令 | 功能說明 |
|-----|---------|
| `推薦` / `選股` / `AI` | 根據 **當前策略** 推薦股票 |
| `V30` / `策略` | 使用 V30 純技術分析選股 |
| `2330`（4碼） | 個股 AI 健康診斷（三維度分析） |

#### 推薦流程說明

1. **策略篩選**：根據你選擇的策略（V30/V33/V35）進行第一輪篩選
2. **AI 排名**：使用 XGBoost 模型對候選股評分
3. **Top 5 輸出**：返回評分最高的前 5 檔股票

#### 示例輸出

```
🎯 【V33 低波動策略】推薦
日期：2026-02-12

1. 2330 | $550.00
   RSI: 65.3

2. 2454 | $123.50
   RSI: 58.2

...

📊 共篩選出 12 檔
⚠️ 僅供參考，請自行評估風險
```

---

### 3. 個股診斷（AI 健康診斷書）

輸入任意 **4 碼股票代號**，例如 `2330`，即可獲得：

```
🏥 【個股健康診斷】2330 台積電

📊 技術面：
• 收盤價：$550
• MA 趨勢：多頭 ↗️
• RSI：65 (中性偏強)

💼 基本面：
• 營業利益率：38.5% (優異)
• 營收 YoY：+12.3%

🤖 AI 評分：85/100
💬 智能評語：基本面優異，技術面強勢，建議關注

⚠️ 風險提示：本診斷僅供參考
```

---

### 4. V30 參數調整

| 指令 | 功能 | 範例 |
|-----|------|------|
| `設定停損 5` | 設定停損比例 | 設為 5%（範圍 1-20%） |
| `設定停利 20` | 設定停利比例 | 設為 20%（範圍 5-50%） |
| `設定停利 0` | 取消停利 | 持有至停損或到期 |
| `查看設定` | 查看當前參數 | 顯示停損/停利/持有天數 |

---

### 5. V34 / V35 參數調整（新）

| 指令 | 功能 | 範例 |
|-----|------|------|
| `V34積極` / `V34保守` | 一鍵切換 V34 檔位（含嚴格+放寬） | `V34積極` |
| `V35積極` / `V35保守` | 一鍵切換 V35 檔位（含嚴格+放寬） | `V35保守` |
| `設定V34 y b v` | 自訂 V34 嚴格參數 | `設定V34 18 0.93 0.9` |
| `設定V34放寬 y b v` | 自訂 V34 放寬參數 | `設定V34放寬 10 0.90 0.7` |
| `設定V35 o y v` | 自訂 V35 嚴格參數（o 為 %） | `設定V35 6 0 0.8` |
| `設定V35放寬 o y v` | 自訂 V35 放寬參數（o 為 %） | `設定V35放寬 4 -5 0.6` |

參數意義：
- V34：`y=營收YoY(%)`、`b=突破倍率`、`v=量比下限`
- V35：`o=營業利益率(%)`、`y=營收YoY(%)`、`v=量比下限`

---

### 6. 其他指令

| 指令 | 功能 |
|-----|------|
| `dashboard` / `儀表板` | 取得 Web Dashboard URL |
| 任意訊息 | 顯示完整指令清單 |

---

## 🔧 Ngrok 連線設定

### 步驟 1：啟動服務

```powershell
# 啟動 Flask 服務（預設 port 8866）
python app.py
```

### 步驟 2：啟動 Ngrok

```powershell
# 另開終端執行
ngrok http 8866
```

### 步驟 3：設定 Line Bot Webhook

1. 複製 ngrok 提供的 HTTPS URL（例如：`https://abc123.ngrok.io`）
2. 前往 [Line Developers Console](https://developers.line.biz/console/)
3. 設定 Webhook URL：`https://abc123.ngrok.io/callback`
4. 驗證連線（應顯示成功）

---

## 🧪 測試方式

### 方法 1：使用 Postman 模擬

```json
POST http://localhost:8866/callback
Content-Type: application/json

{
  "events": [{
    "type": "message",
    "message": {"type": "text", "text": "切換V33"},
    "replyToken": "test-token",
    "source": {"userId": "test-user"}
  }]
}
```

### 方法 2：直接透過 Line App

1. 確保 ngrok 正在運行
2. Line Webhook 已驗證成功
3. 在 Line 對話框輸入指令（例如：`切換V33`）

---

## ⚠️ 常見問題排除

| 問題 | 可能原因 | 解決方法 |
|-----|---------|---------|
| Line Bot 無回應 | ngrok 未啟動 | 重啟 ngrok 並更新 Webhook URL |
| 輸入「策略」報錯 | 參數讀取失敗 | ✅ 已修復（v0.2.12）- 確保 take_profit 有預設值 |
| 回傳「今日無資料」 | 資料庫未更新 | 執行 `python 1_update_database.py` |
| AI 診斷回傳 None | 財報表無資料 | 執行 `python 7_update_financials.py` |
| 策略切換無反應 | 策略未註冊 | 檢查 `strategy_settings.json` |

---

## 💡 最佳實踐建議

### 每日更新流程

```powershell
python 1_update_database.py    # 更新股價
python 7_update_financials.py  # 更新財報（每季執行即可）
python 2_rundaily.py           # 計算技術指標
python app.py                  # 啟動 Line Bot 服務
```

### 策略選擇建議

| 投資風格 | 推薦策略 | 理由 |
|---------|---------|------|
| 🛡️ 保守穩健 | V33 低波動 | 波動小、風險低 |
| ⚖️ 均衡成長 | V31 混合策略 | 經驗證（ROI 22.86%） |
| 🚀 積極進取 | V34 雙渦輪 | 高營收成長 + 價格突破 |
| 💼 價值投資 | V35 經營效益 | 高營業利益率、中長線 |

### 策略對比表

| 策略 | 停損 | 停利 | 持有天數 | 特色 | 適用者 |
|-----|-----|-----|--------|------|-------|
| V31 混合 | 7% | 15% | 10 天 | 均線+RSI+AI | 一般投資人 |
| V33 低波動 | 5% | 12% | 15 天 | NATR<4%、低波動 | 保守型 |
| V34 雙渦輪 | 8% | 20% | 7 天 | 營收YoY>30% | 積極型 |
| V35 經營效益 | 8% | 20% | 30 天 | 營業利益率>10% | 價值型 |

### 使用情境範例

#### 情境 1：日常選股
```
使用者: 查看策略
Bot: 顯示當前為 V31 混合策略

使用者: 推薦
Bot: 返回 5 檔 V31 推薦股票
```

#### 情境 2：切換策略
```
使用者: 切換V33
Bot: 已切換至 V33 低波動策略

使用者: 推薦
Bot: 返回 5 檔 V33 低波動推薦股票
```

#### 情境 3：個股確認
```
使用者: 2330
Bot: 顯示台積電的 AI 健康診斷書
```

---

## 📊 策略對比表

| 策略 | ROI | MDD | Sharpe | 特色 | 適用者 |
|-----|-----|-----|--------|------|-------|
| V30/V31 | 22.86% | 13.01% | 1.16 | 均線突破 + AI | 一般投資人 |
| V33 | - | 低 | - | 低波動 | 保守型 |
| V35 | - | - | - | 研發動能 | 成長股愛好者 |

> **註**：V33/V35 的回測數據請執行 `/backtest` 查看

---

## 🔄 更新日誌

### v0.3.0 (2026-02-12)
- ✅ 修復策略切換失效問題（改用 StrategyManager 寫入 JSON 而非 DB）
- ✅ 「推薦」指令現在會依當前策略篩選 + AI 排名
- ✅ 輸出詳細資訊（收盤價、AI分數、RSI、量能、停損停利價）
- ✅ 新增 V34 雙渦輪策略切換指令
- ✅ 幫助選單顯示當前使用策略
- ✅ 前端近期交易表新增「策略」欄位 + 策略篩選下拉

### v0.2.12 (2026-02-12)
- ✅ 修復 `NoneType` 錯誤（take_profit 參數）

### v0.2.11 (2026-02-11)
- 新增個股 AI 診斷功能
- 改善前端按鈕樣式

---

## 📞 技術支援

如有問題，請檢查：
1. `.env` 檔案的 `LINE_CHANNEL_SECRET` 和 `LINE_CHANNEL_ACCESS_TOKEN` 是否正確
2. 資料庫是否已初始化（`python init_settings.py`）
3. ngrok 是否正常運行
4. Webhook URL 是否設定正確

**提示**：執行 `python app.py` 時，終端會顯示啟動資訊和模型載入狀態。
